//Tutorial 21 - FETCH DATA USING HTTP

export interface IEmployee { //this file has created an type for which the observable can be cast into - remember to import into the service
    id: number,
    name: string,
    age: number
}